

@react.component
let make = () => {

  <section>
    <header>
      <h2>{ React.string("Home") }</h2>
    </header>
    <p>{ React.string("Little demo implemented with ReScript") }</p>
    <p>{ React.string("Check the two pages with examples") }</p>
  </section>
}